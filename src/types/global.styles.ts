import { StyleSheet } from "react-native";
const GlobalStyle = StyleSheet.create({
  globalTimeText: {
    fontSize: 12,
    fontWeight: "500",
    color: "#64748B",
  },
});

export default GlobalStyle;
